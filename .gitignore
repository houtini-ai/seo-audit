node_modules/
dist/
build/
.env
.env.*
!.env.example

# Secrets — NEVER commit. API scope notes + credential pointers live here.
.secrets/
*.key
*-credentials.json
gsc-access-*.json

# Local Claude Code session config
.claude/

# Local databases / crawl output
*.db
*.db-wal
*.db-shm
seo-audits/
crawlee-storage*/

# Crawl artefacts — large, regenerable
crawl-tests/**/*.json
crawl-tests/**/*.html
crawl-tests/**/raw/

# Build/tooling caches
*.tsbuildinfo
.vite/
*.log

# OS
.DS_Store
Thumbs.db
.preview/

# Internal research workspace (kept locally, not part of the public repo)
research/

# Internal working docs — not part of the public repo (kept locally)
CLAUDE.md
plan/
findings/
/audit/
crawl-tests/
docs/writer-brief.md

# Docs are maintained outside the public repo
docs/

# scripts/ — only the public smoke test + documented probes are tracked; internal
# one-off diagnostics and property-specific QA harnesses stay local
scripts/*
!scripts/smoke.mjs
!scripts/qa-crawl.mjs
!scripts/probe-crawl.mjs
!scripts/probe-gsc.mjs
!scripts/probe-fixfinding.mjs
!scripts/probe-schema.mjs
!scripts/probe-schema-rules.mjs
!scripts/probe-extract.mjs
!scripts/probe-extract2.mjs
!scripts/probe-backlinks.mjs
!scripts/probe-dfs-labs.mjs
!scripts/probe-6a.mjs
!scripts/probe-6b.mjs
!scripts/probe-6c.mjs
!scripts/probe-6d.mjs
!scripts/probe-priority.mjs
!scripts/probe-templates.mjs
!scripts/probe-migrate.mjs
!scripts/probe-boot.mjs
